//This is the first file created to practice different Git commands.
// First step is to git inti. For that we write git init on the terminal.
// To check if the git has ben initialized we write git command on the terminal and press OK. 
// After that type git add .
//  After adding, commit the git by typing  and entering git commit -m"whatever commit message is" 

